# Dev notes
1. Have a database named `balance` created from phpmyadmin prior 
2. (Optional) load the `debug_insert()` from `authz.php` data before running
3. Start from `home.php`

Made with no purpose ♥️
